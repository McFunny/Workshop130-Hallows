using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BugBehaviorScript : MonoBehaviour
{
    //Split behavior into flying, fleeing, wandering, moving to target, and leaving (Burrow, poof, or fly away)
    //Def potential here for a leaf cutter bug that the player can automate their defense by catching enough for foxglove pesticide

    public InventoryItemData bugItem;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
